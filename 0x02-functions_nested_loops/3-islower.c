#include "holberton.h"
/**
* main - Write a function that checks for lowercase character.
*
* Return: 0 
*/
int _islower(int c)
{
	
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
