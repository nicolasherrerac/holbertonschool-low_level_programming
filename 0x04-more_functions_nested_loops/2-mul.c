#include "holberton.h"
/**
* mul - multiplies two integers.
* @a: Variable of multiplies
* @b: Variable of multiplies
* Return: 0
*/
int mul(int a, int b)
{
	int res;

	res = a * b;

	if ((a > 0) && (b > 0))
	{
		return (res);
	}
	else
	{
		return (res);
	}
	return (0);
}
